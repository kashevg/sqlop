# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=sqlop
DB_USER=sqlop_user
DB_PASSWORD=sqlop_password

# Google Cloud / Gemini Configuration
# Option 1: Using GCP project with Vertex AI
GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1

# Option 2: Using API Key (for development)
# GOOGLE_API_KEY=your-api-key

# Gemini Model Configuration
GEMINI_MODEL=gemini-2.0-flash-exp
GEMINI_TEMPERATURE=0.7

# Langfuse Observability & Security Monitoring (Local)
# After starting docker-compose, visit http://localhost:3000 to:
# 1. Create an account
# 2. Create a project
# 3. Get your API keys from project settings
LANGFUSE_PUBLIC_KEY=pk-lf-your-public-key-from-localhost-3000
LANGFUSE_SECRET_KEY=sk-lf-your-secret-key-from-localhost-3000
LANGFUSE_HOST=http://localhost:3000
LANGFUSE_TRACING_ENVIRONMENT=development
LANGFUSE_ENABLED=true